#include <stdio.h>
#include <string.h>

int main() {

  char s1[] = "Hello";
  char s2[] = "";
  
  size_t len_s1 = strlen(s1);
  printf("%s", s1);

  while (len_s1 != -1) {
    len_1--1;
    printf(len_1);
  }
  return 0;
}